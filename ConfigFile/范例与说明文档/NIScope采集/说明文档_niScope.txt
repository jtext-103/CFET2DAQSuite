{
    //采集卡名，对应NIMAX中的采集卡名
    "ResourceName":"PXI1Slot7",

    "TriggerConfig": {
      //触发类型，0：立即触发（Ready了马上就变Running，但作为从卡不会）；1、数字边沿触发；2、边沿触发
      "TriggerType": 0,
      //触发源，触发类型为 0 时设置为 0；触发类型为 1 时设置为 "VAL_PFI_1"；触发类型为 2 时可设置为 "0"-"7"
      "TriggerSource": "0",
      //触发沿，0：上升沿；1：下降沿
      "TriggerEdge": 0,
      //主从	，0：无主从；1：主卡；2：从卡
      "MasterOrSlave": 0
    },

    "ClockConfig": {
      //采样率
      "SampleRate": 30000000.0,  
      //总采样点数
      "TotalSampleLengthPerChannel": 30000000
    },

    "ChannelConfig": {
      //通道名，用通道号的数组表示
      "ChannelName": [
          0,1,2,3,4,5,6,7
      ],
      //最小值
      "MinimumValue": 0.0,
      //最大值
      "MaximumValue": 10.0
    },

    //以下配置对采集卡没有影响，但对采集程序的行为有影响
    
    //采样通道数，必须与ChannelName通道数相同！！！
    "ChannelCount": 8,
    //采集开始时间，作为元数据存入采集文件，决定数据的时间轴
    "StartTime":0.5,
    //采集结束后自动存文件，true：存；false：不存，无数据
    "AutoWriteDataToFile": true,   
    //本地保存的最大炮数，超过这个数就删除到RemainShotsMin
    "RemainShotsMax": 30,
    //本地保存的最小炮数
    "RemainShotsMin": 20
}

常见问题：
1、绝对不能出现中文字符
3、文档仔细看，不行问王昱星
4、通道号注意别搞错了，可以参考下NIMAX里面看到的内容
5、这是个JSON格式文件，编辑之后最好去http://www.bejson.com/或类似网站检查一下是否正确

